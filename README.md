# RoboMaster 操作员客户端 - 招新面试作答项目
本项目为华南农业大学Taurus战队视觉组前后端开发方向招新面试题**Q1 自定义客户端界面开发**的完整作答实现，基于 Qt Quick 6 开发，完整还原 RoboMaster 机甲大师赛操作员客户端的核心功能与交互逻辑。

> 项目开发周期：10天，从零开始学习 Qt Quick 与 QML 语法，边学边做完成全功能开发与调试，ai辅助。

---

## 项目概述
本项目针对 RoboMaster 高校赛的实际比赛场景，开发了一套叠加在机器人第一视角图传画面上的操作员客户端界面，实现了以下核心能力：
1.  比赛全局状态、双方机器人血量、己方机器人核心数据的实时可视化展示
2.  符合官方简化通信协议的鼠标控制指令下发（移动速度、滚轮、左/右/中键状态）
3.  底盘性能、发射模式的快速切换控制，以及比赛暂停/恢复功能
4.  子线程模拟比赛数据实时变化，主线程UI无卡顿响应
5.  适配比赛紧张场景的视觉动效与交互优化，关键数据一眼可辨

项目完全遵循题目要求，使用 CMake 构建工程，无真实网络通信依赖，完整跑通了「数据模拟-界面渲染-指令下发-控制台反馈」的全链路。

---

## 项目环境与编译运行
### 依赖环境
| 依赖项 | 版本要求 |
|--------|----------|
| Qt     | 6.8.0 及以上（需包含 Qt Quick 模块） |
| CMake  | 3.16 及以上 |
| 编译器 | Windows: MSVC 2022 / MinGW 11.2+；Linux/macOS: GCC 11+ |

### 编译与运行步骤
#### Windows 平台（Qt Creator）
1.  打开 Qt Creator，点击「打开文件或项目」，选择本项目的`CMakeLists.txt`
2.  选择合适的编译套件（Qt 6.8.0 对应的MSVC/MinGW），完成项目配置
3.  点击左下角「运行」按钮，即可自动编译并启动程序

#### 命令行编译（全平台通用）
1.  确保 Qt 环境变量已配置，`qmake`/`cmake` 可在命令行访问
2.  进入项目根目录，执行以下命令：
    ```bash
    # 创建构建文件夹
    mkdir build && cd build
    # 生成构建文件
    cmake ..
    # 编译项目
    cmake --build .
    # 运行程序（Windows）
    ./Debug/appRM.exe
    # 运行程序（Linux/macOS）
    ./appRM
    ```
---

## 项目结构说明
```
RM-Operator-Client/
├── CMakeLists.txt          # 项目CMake构建配置（符合题目要求，不使用QMake）
├── main.cpp                # 程序入口，初始化Qt应用、QML引擎、C++桥接类
├── rmbridge.h              # 核心桥接类头文件，定义QML可访问的属性与槽函数
├── rmbridge.cpp            # 桥接类实现，包含控制逻辑、数据接收、指令打印、线程管理
├── Main.qml                # 主界面QML文件，所有UI元素、交互逻辑、动画效果实现
├── res.qrc                 # Qt资源文件，管理图传背景图片等静态资源
└── image/              # 图片资源
     └── sb.png          # RoboMaster第一视角图传背景图
```

### 核心文件职责说明
1.  **`rmbridge.h/cpp`**：项目核心C++后端，承担三大职责
    - 数据桥接：通过`Q_PROPERTY`将C++数据暴露给QML界面，实现双向绑定
    - 线程管理：启动子线程`DataGenerator`模拟比赛数据，保证UI线程不卡顿
    - 指令处理：接收QML的鼠标、按键、模式切换操作，输出控制台日志，实现题目要求的操作反馈
2.  **`Main.qml`**：前端UI实现，完全遵循Qt Quick声明式语法，所有UI元素叠加在图传背景之上，保留核心视野完整性
3.  **`main.cpp`**：程序入口，规范初始化Qt应用，将C++桥接类实例暴露给QML上下文，保证前后端通信链路通畅
4.  **`CMakeLists.txt`**：工程构建配置，适配Qt 6标准构建流程，跨平台兼容

---


## 核心功能实现
| 功能模块 | 实现详情 |
|----------|----------|
| 比赛状态展示 | 红/蓝双方比分、倒计时、基地血条、6台机器人单位小血条完整实现 |
| 己方机器人数据 | 血量、弹量、武器热量、经验等级、缓冲能量、射击初速度、脱战状态实时更新 |
| 鼠标控制协议 | 完全匹配官方通信协议，实现鼠标坐标转移动速度、滚轮速度、三按键状态监听 |
| 控制指令交互 | 底盘性能模式切换、发射模式切换、左键开火、ESC全局暂停/恢复 |
| 多线程数据模拟 | 子线程独立生成比赛数据，主线程UI无卡顿渲染，线程安全通信 |
| 视觉动效优化 | 低血量闪烁、受击闪红+屏幕抖动、模式切换高亮、状态变化视觉反馈 |

---


### 基础操作说明
- 鼠标移动：控制云台移动速度，控制台实时输出速度值
- 鼠标左键：按下开火，连发模式下持续开火，控制台输出开火指令
- 鼠标滚轮：滚动调整，控制台输出滚轮速度值
- 鼠标右键/中键：按下控制台输出对应按键状态
- 左下角按钮：点击打开底盘/发射模式选择窗口，切换对应模式
- ESC键：全局暂停/恢复比赛，暂停时所有操作失效，子线程数据生成停止

---

## 核心作答思路与架构设计
### 整体架构设计思路
针对题目要求与RoboMaster实际比赛场景，我采用了**「UI主线程 + 数据子线程」的分层架构**，完全遵循Qt的线程安全规范：
1.  **主线程（GUI线程）**：只负责QML界面渲染、用户交互事件接收，不做任何耗时计算，保证界面永远流畅不卡顿，符合比赛场景的低延迟要求
2.  **子线程（数据线程）**：独立运行定时器，每秒生成模拟比赛数据，通过Qt信号槽将数据发送给主线程，完全不接触UI对象，避免了跨线程操作UI的崩溃风险
3.  **桥接层设计**：`RMBridge`类作为前后端通信的唯一桥梁，封装了所有QML可访问的属性和槽函数，实现了界面与逻辑的解耦，代码可维护性更高

### 核心模块设计逻辑
#### 1. 数据绑定与实时更新
通过Qt的`Q_PROPERTY`宏，将C++端的所有比赛数据暴露给QML，数据变化时通过`dataUpdated`信号通知QML自动刷新界面，无需手动调用更新函数，保证了数据与界面的实时同步，完全符合题目「数据实时响应」的要求。

#### 2. 鼠标控制协议适配
针对题目给出的`MouseControl`上行协议，我做了针对性的适配实现：
- 坐标转速度：通过「当前鼠标坐标 - 上一帧坐标」计算相对位移，在固定发送频率下等价于移动速度，完全匹配协议定义
- 方向适配：严格遵循协议要求，x轴负值向左、y轴负值向下、滚轮负值向后滚动
- 按键监听：开启MouseArea的全按键响应，实现左/右/中键的状态监听，按下/松开全生命周期闭环
- 高频操作节流：对鼠标移动、滚轮、按键操作做了50ms的节流处理，避免操作过快导致的指令泛滥，解决了题目提出的「操作过快」问题

#### 3. 交互与视觉优化
针对题目「关键数据一眼辨别、保留视野完整性」的要求，我做了以下优化：
- 所有UI元素采用半透明背景，叠加在图传画面上，不遮挡核心视野
- 低血量、超热量等危险状态采用颜色变化+闪烁动画，操作员无需仔细看数字即可快速识别
- 受击时添加闪红+屏幕抖动动画，还原真实比赛的操作反馈
- 模式选择窗口点击空白处即可关闭，操作流程极简，适配比赛紧张场景

### 考核完成度总结
本项目**100%覆盖了题目Part A、Part B的所有考核要求**，无功能遗漏，同时完成了多项超出题目要求的优化：
| 题目考核点 | 完成情况 |
|------------|----------|
| Part A 界面元素叠加在图传背景上 | ✅ 完全实现，所有UI均叠加在第一视角图传上 |
| 比赛状态与机器人关键数据展示 | ✅ 完整覆盖协议所有字段，布局合理 |
| 关键数据状态变化一眼辨别 | ✅ 颜色变化+动画反馈，危险状态醒目 |
| 界面下达控制指令 | ✅ 模式切换、开火、鼠标控制、暂停全实现 |
| 数据变化实时响应 | ✅ Q_PROPERTY双向绑定，信号触发实时刷新 |
| 保留视野完整性，复杂背景下信息清晰 | ✅ 半透明UI，不遮挡核心视野，文字高对比度 |
| 状态变化配视觉动效 | ✅ 低血闪烁、受击闪红+抖动、模式高亮全实现 |
| Part B C++类接入QML界面 | ✅ RMBridge桥接类规范实现，上下文正确暴露 |
| 主线程UI+子线程数据生成 | ✅ 子线程独立运行，线程安全信号槽通信 |
| 操作时控制台输出对应指令 | ✅ 所有鼠标、按键、模式切换、暂停操作均有规范打印 |
| 操作过快的处理 | ✅ 50ms节流处理，避免高频操作性能问题 |
| CMake构建工程 | ✅ 完全使用CMake，不使用QMake |
| 模拟数据，无需真实网络 | ✅ 子线程完整模拟数据，全链路跑通 |

**额外加分项实现**：
- 完整的机器人等级经验系统，支持溢出经验自动升级，贴合真实比赛规则
- 完整的受击反馈动画系统，区分普通血量受击与低血受击的不同效果
- 线程安全的暂停/恢复机制，子线程定时器可安全启停
- 修复Qt新版语法弃用警告，代码符合最新Qt规范
- 完整的开火逻辑，子弹数量实时扣减，子弹耗尽有对应提示

---

## 效果展示
`[请在此处粘贴项目运行截图，建议包含以下内容]`
1.  主界面运行截图（完整UI+图传背景）
2.  模式切换窗口截图
3.  暂停界面截图
4.  控制台指令输出截图
5.  （可选）项目运行演示视频链接

---

## 项目成长与收获
`[请在此处填写你自己的开发经历、遇到的困难、解决过程、学习收获]`
示例参考：
> 作为大一新生，在开发这个项目之前，我完全没有接触过Qt、QML，甚至对C++的多线程编程也只有课本上的理论了解。10天的开发过程中，我从Qt Quick的入门文档开始学起，一步步搭建项目框架，解决了一个又一个问题：从最开始QML和C++数据无法同步，到后来子线程操作UI导致的程序崩溃，再到鼠标按键无响应、Qt语法警告、坐标转速度的逻辑理解等等。
> 
> 这个项目让我真正理解了「边做边学」的意义，不仅掌握了Qt Quick的开发流程、QML与C++的交互方式、多线程编程的线程安全规范，更深入理解了RoboMaster比赛中操作员客户端的设计逻辑，对工程化代码开发有了全新的认知。
> 
> 同时我也认识到自己还有很多不足，比如代码的可扩展性还可以进一步优化，对Qt的高级特性还不够熟悉，后续我会继续深入学习，也非常希望能加入Taurus战队，和大家一起学习成长，在赛场上取得好成绩。

---

## 个人信息
- 姓名：朱迅
- 年级：2025级 大一
- 学院：数学与信息学院 软件学院



## 致谢
感谢华南农业大学Taurus战队提供的本次招新面试题，让我在短时间内得到了极大的成长，对机器人工程、客户端开发有了更深入的理解。
